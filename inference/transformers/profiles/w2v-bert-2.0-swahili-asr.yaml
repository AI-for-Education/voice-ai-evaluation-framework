# Source: https://huggingface.co/badrex/w2v-bert-2.0-swahili-asr
id: w2v-bert-2.0-swahili-asr
framework: transformers
adapter: ctc
artifact: w2v-bert-2.0-swahili-asr
language: sw
task: transcribe
output_units: orthographic
pipeline_contract:
  schema_version: 1
  audio_preparation: shared_soundfile_librosa_16khz
  inference:
    artifact: canonical_checkpoint
    frontend: canonical_transformers_auto_processor
    runtime: shared_transformers_image
    chunking: none
  evaluation: direct_pred_text_by_output_units
  runtime_resolution: observed_effective_values_v1
loader:
  processor_mode: auto
  local_files_only: true
  trust_remote_code: false
  torch_dtype: auto
decoding:
  strategy: greedy
  generation_kwargs: {}

parameter_evidence:
  - applies_to:
      - language
      - decoding.strategy
    level: exact
    source: https://huggingface.co/badrex/w2v-bert-2.0-swahili-asr
    rationale: The checkpoint is published for Swahili CTC ASR and uses greedy decoding as the tracked baseline.
